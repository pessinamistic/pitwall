# local working notes — contain machine-specific details, not part of the public repo
IMPLEMENTATION_PLAN.md
chatgpt_output.txt

# editors / OS
.idea/
.vscode/
*.un~
.DS_Store

# node
node_modules/

# local git worktrees parked by EnterWorktree / fleet tooling — never committed
# (a nested checkout here contains a .git file with an absolute gitdir path)
.claude/worktrees/

# benchmark output (keep the directory, ignore the runs)
benchmarks/results/*
!benchmarks/results/.gitkeep

# local backups made by install.sh
*.bak

# tech-lead session handoff notes — not part of the public repo
TECH_LEAD_HANDOFF.md

# bearings status snapshots — regenerated on demand, not shared history
.agents/bearings-*.md
