# local working notes — contain machine-specific details, not part of the public repo
IMPLEMENTATION_PLAN.md
chatgpt_output.txt

# editors / OS
.idea/
.vscode/
*.un~
.DS_Store

# node
node_modules/

# benchmark output (keep the directory, ignore the runs)
benchmarks/results/*
!benchmarks/results/.gitkeep

# local backups made by install.sh
*.bak

# tech-lead session handoff notes — not part of the public repo
TECH_LEAD_HANDOFF.md

# bearings status snapshots — regenerated on demand, not shared history
.agents/bearings-*.md

# ephemeral worktrees created by the concurrent-session isolation policy
.claude/worktrees/

# scratch output from image-generation sessions. The real assets live in
# assets/generated/ (committed, human-reviewed) — see docs/visual-identity.md §5.
sample_*_image_*.jpg
generated-images/
